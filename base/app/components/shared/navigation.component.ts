import { Component } from '@angular/core';
import { ModalInfo, ModalFormType, ModalCommand, ModalType, ModalLocation } from "./../../entities/modal.entity";
import { IModalReturnValue } from "./../../entities/modal_alt.entity";
import { environment } from "./../../../environments/environment";

@Component({
    selector: 'e-navigation',
    templateUrl: './templates/navigation.template.html'
})

export class NavigationComponent {

    constructor(
    ){  
    }

}